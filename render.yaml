services:
  - type: web
    name: qsentry
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m sih141.web --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
